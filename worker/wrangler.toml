name = "animepahe-worker"
main = "src/worker.ts"
compatibility_date = "2025-08-11"

# Optional — enable modules syntax if needed
# type = "javascript"

[vars]
API_URL = "https://example.com"  # If your Worker needs env vars
